# 1단계 Actions 시험 결과 — 2026-09-14

[시험 실행 34787754917](https://github.com/Jay-Hong/Recru_It_JSON/actions/runs/34787754917)은
`test/stage1-observation` 브랜치에서 성공했다. 수집·검증·통계 업로드와 해당 브랜치의
결과 커밋·푸시까지 완료됐다. master는 `6338eff`를 유지했다.

- 시험한 코드: `34ad3b154c07f043a89ebcd69eda51a48c7c6809`
- 자동 생성된 결과 커밋: `fe1d222a92abbcc74546581a8e8634a9942dec59`
- 실행 통계 ID: `34787754917-1`
- 통계 artifact: `recru-stats-34787754917-1` (14일 보관)

## 실행과 수집 결과

| 항목 | 결과 |
| --- | ---: |
| 전체 작업 시간 | 31분 15초 |
| Execution 단계 | 30분 25초 |
| Linux 자동 테스트 | 32개 통과, 10.758초 |
| 스크롤 / 목록 카드 | 53회 / 885개 |
| 지역 순회 | 17개 모두 완료 |
| 수집 대상 / 시도 수 | 478건 / 494회 |
| 검증 성공 / 최종 실패 | 477건 / 1건 |
| 클릭 가림 | 15회, 모두 재시도로 복구 |
| 재시도 | 16회 중 15회 성공 |
| 기존 텍스트 형식 비교 | 477건 일치, 불일치 0건 |
| 수동 공고 / 필터 제외 / 최종 저장 | 2건 / 159건 / 320건 |
| Scrapy 예외 / 관측 장치 오류 | 0건 / 0건 |
| 사이트 429·503 응답 | 0건 |

검증 성공 477건 + 수동 2건 = 저장 320건 + 필터 제외 159건으로 집계가 일치한다.
결과 JSON 3개는 서로 완전히 같으며, 내려받은 실행 통계와 로컬 검증기로도 재검증했다.
실행 후 코드와 워크플로는 시험 대상 코드 커밋과 동일했다.

경기의 공고 1건은 시도 125와 재시도 126에서 모두 `identity` 검증에 실패해 저장하지
않았다. 이 조건은 상세 객체 미갱신·부재 또는 ID 불일치를 포함한다. 현재 기록만으로
어느 경우인지, 원래 공고에 어떤 문제가 있었는지는 확정할 수 없다. 두 시도 모두 상세
HTTP 요청은 있었고 200 응답을 받았다. HTTP 성공만으로 공고 검증을 통과시키지 않았다.
이후 공고와 남은 지역의 수집은 계속됐으며, 최종 실패 1건을 알리는
`Collection quality` 경고가 GitHub 실행 화면에 실제로 생성된 것을 확인했다.

## Linux에서 측정한 준비 시간과 요청 수

| 측정 | 중앙값 | 95백분위 | 최대 |
| --- | ---: | ---: | ---: |
| 클릭 → 검증 가능한 화면 (477건) | 0.234초 | 0.385초 | 0.436초 |
| 연결된 요청 → 응답 헤더 (479회) | 0.190초 | 0.344초 | 0.401초 |

두 시간은 각각 페이지 시계와 CDP 시계 안에서 계산했다. 응답 479회에는 최종 실패한
공고의 두 시도도 포함하며, 화면 준비 시간에는 검증에 성공한 공고만 포함한다.

수집 중 상세 요청은 POST 479회 + OPTIONS 226회 = 705회였다. 클릭 가림 15회는
상세 POST를 만들지 않았으며, 전체 시도 494회에서 이를 뺀 값과 POST 수가 일치한다.
목록 요청은 85회, 기타 사이트 요청은 166회였다. 수집 중 사이트 요청 전체의
움직이는 60초 최대치는 88회였고, 상세 요청만의 최대치는 38회였다.
이 값은 기존 대기를 유지한 1단계의 관측치이며, 2.2초 간격의 시험 결과가 아니다.

## 로그와 결과 반영

- 직전 cron의 전체 로그: 19,772,181바이트, 브라우저 응답 출력 15,532줄.
- 시험의 전체 로그: 1,131,685바이트, 브라우저 응답 출력 0줄, 응답 본문 조회 명령 0건.
- 로그 크기는 약 94.3% 줄었다. 기존 Scrapy의 공고·필터 로그는 그대로 남는다.
- 통계 문자열 값 검사에서 연락처 패턴은 발견되지 않았다. 원문 응답이나 공고 내용은
  통계에 저장하지 않는다.
- 일반 `gh run view --log` 조회가 직전 cron 로그의 일부만 반환해, 비교에는 두 작업 모두
  `actions/jobs/{job_id}/logs`에서 받은 전체 로그를 사용했다.
- 코드 커밋에는 결과 JSON 변경이 없다. 결과 커밋 `fe1d222`는 JSON 3개만 변경했다.
  이후 master 반영에는 코드 커밋 `34ad3b1`을 사용하고 시험 결과 커밋은 제외해야 한다.

직전 cron은 전체 29분 34초, 수집 29분 3초, 저장 309건이었다. 이번에는 수집 대상과
검증 성공 수가 달랐고 재시도·검증도 추가됐으므로, 전체 시간 차이만으로 성능 변화를
단정할 수 없다. 이번 시험은 현재 1단계 코드의 첫 전체 Linux 실행이다.

## Claude 검토에 대한 판단

- 오류 분리, 검증된 스냅샷만 저장, 로그 억제, Selenium 고정에 관한 평가는 코드와 시험으로 확인했다.
- 최종 실패/형식 불일치 경고는 반영했다. 형식 불일치의 재시도 생략과 임시 실패율 10% 기준은 적용하지 않았다.
- 시험 결과 커밋을 master에 합치지 말라는 권고는 타당하다.
- 다만 현재 master의 기존 워크플로에는 concurrency 설정이 없으므로, 시험과 cron이
  겹치면 한쪽이 자동 대기한다는 설명은 현재 상태에는 맞지 않는다. 이번에는 실행 중인
  다른 작업이 없었고, 다음 정기 실행과 충분히 떨어진 시간에 시험했다.

정기 실행 3~5회의 비교와 후속 속도 조정은 아직 수행하지 않았다.
